#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../http-console/bin/http-console" "$@"
  ret=$?
else 
  node  "$basedir/../http-console/bin/http-console" "$@"
  ret=$?
fi
exit $ret
